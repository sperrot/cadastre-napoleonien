# Sorties générées par le harvester (à régénérer)
harvest/seed_*.sql
harvest/*.log

# Python
__pycache__/
*.pyc

# Divers
.DS_Store
Thumbs.db

# Cache disque du harvester
harvest/.cache/
# Fichiers COG INSEE téléchargés par build_alias_insee.py (14 Mo, régénérables)
harvest/_cog/
# Cache des manifestes IIIF par departement (regenerable, ~18 Mo pour l'Aisne)
harvest/_manifestes_*.json

# Secrets locaux (clé service_role Supabase, etc.)
.env
*.env

# Données sources brutes (téléchargées, volumineuses, à régénérer)
download/

# Config/session locale de l'agent
.claude/

# Cache de build Cloudflare Workers
.wrangler/
proxy/**/.wrangler/

# Explorations hors projet (vectorisation BD Carto etat-major)
19siecle.qgz
harvest/wfs_vigne_etatmajor.py
